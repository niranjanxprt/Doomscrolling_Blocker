services:
  - type: web
    name: doomscrolling-blocker-web
    runtime: python
    buildCommand: "pip install -r web/requirements.txt"
    startCommand: "cd web && uvicorn app:app --host 0.0.0.0 --port $PORT"
    envVars:
      - key: PYTHON_VERSION
        value: 3.13.0
